query allTasks {
  allTasks {
    id
    title
    description
    completed
    createdDate
  }
}

mutation createTask($input: CreateTaskInput!) {
  createTask(input: $input) {
    task {
      id
      title
      description
      completed
      createdDate
    }
  }
}
